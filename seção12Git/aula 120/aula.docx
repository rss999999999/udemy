$ git --version
$ git config --global user.name "Rodrigo"
$ git config --global user.email "rss.999999999@gmail.com"
$ git config --list
$ ssh-keygen -t ed25519 -C "rss.999999999@gmail.com"
$ ls
$ git init
$ git add .
$ git status
$ git commit -m "Criadas páginas index e vendas"
$ git branch -M main
$ git remote add origin git@github.com:rss999999999/aulagithub.git
$ git push -u origin main
$ git clone git@github.com:rss999999999/aulagithub.git
$ cd aulagithub/
$ code .
$ git commit -m "Criada página de blog"
$ git log
$ git reset
$ git log --oneline
$ git diff vendas.html
$ git log --oneline
$ git checkout 7767e27
$ git status
$ git log --oneline
$ git reset
$ git clean -df
$ git checkout -- .
$ git checkout main
git reset --soft HEAD~1
git status > confiro o status
git reset > tiro da stage (verde para o vermelho)
git clean -df  > desfaço toda a merda
git checkout -- .
